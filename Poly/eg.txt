id x = x.

swap f x y = f y x.

const x y = x.

not b = if b then False else True.

fac n = if eq n 0
	    then 1
		else mul n (fac (sub n 1)).

fib n = if eq n 0
        then 1
		else if eq n 1
		  then 1
		  else add (fib (sub n 1)) (fib (sub n 2)).

fix f = f (fix f).
